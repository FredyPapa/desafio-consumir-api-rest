import { DevelopersNamePipe } from './developers-name.pipe';

describe('DevelopersNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DevelopersNamePipe();
    expect(pipe).toBeTruthy();
  });
});
