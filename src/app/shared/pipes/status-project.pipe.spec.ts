import { StatusProjectPipe } from './status-project.pipe';

describe('StatusProjectPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusProjectPipe();
    expect(pipe).toBeTruthy();
  });
});
